.page {
  min-height: 100vh;
  background: linear-gradient(180deg, var(--cd-bg) 0%, var(--cd-surface2) 30%, var(--cd-bg) 100%);
}

.topbar {
  height: 88rpx;
  padding: 0 32rpx;
  display: flex;
  align-items: center;
  justify-content: space-between;
}
.title { font-size: 36rpx; font-weight: 600; color: var(--cd-t1); }
.settings-icon {
  width: 64rpx;
  height: 64rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 36rpx;
  color: var(--cd-t2);
}

.scroll {
  height: calc(100vh - 88rpx - 100rpx);
}

/* 用户卡片 */
.profile-card {
  display: flex;
  align-items: center;
  background: var(--cd-surface);
  margin: 0 32rpx 24rpx;
  border-radius: 24rpx;
  padding: 32rpx;
  gap: 24rpx;
  box-shadow: 0 4rpx 24rpx rgba(212, 87, 42, 0.04);
}
.avatar {
  width: 112rpx;
  height: 112rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, var(--cd-accent), var(--cd-accent));
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
}
.avatar-img { width: 100%; height: 100%; }
.avatar-fallback { color: var(--cd-surface); font-size: 48rpx; font-weight: 600; }
.profile-info { flex: 1; line-height: 1.4; }
.profile-name { font-size: 36rpx; font-weight: 600; color: var(--cd-t1); }
.profile-meta { font-size: 24rpx; color: var(--cd-t3); margin-top: 8rpx; }
.profile-arrow { color: var(--cd-t3); font-size: 36rpx; }

/* 通用卡片 */
.card {
  background: var(--cd-surface);
  margin: 0 32rpx 24rpx;
  border-radius: 24rpx;
  padding: 32rpx;
  box-shadow: 0 4rpx 24rpx rgba(212, 87, 42, 0.04);
}
.card-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 16rpx;
}
.card-title {
  font-size: 28rpx;
  font-weight: 500;
  color: var(--cd-t1);
  display: flex;
  align-items: center;
}
.card-icon { margin-right: 12rpx; font-size: 28rpx; }
.card-arrow { color: var(--cd-t3); font-size: 32rpx; }

.school-name {
  font-size: 36rpx;
  font-weight: 600;
  color: var(--cd-accent);
  margin-top: 8rpx;
}
.school-empty { font-size: 26rpx; color: var(--cd-t3); }

.stats-card .card-head { margin-bottom: 24rpx; }
.stats-row {
  display: flex;
  align-items: center;
}
.stat {
  flex: 1;
  text-align: center;
}
.stat-num {
  font-family: "SF Pro Display", sans-serif;
  font-size: 56rpx;
  font-weight: 700;
  color: var(--cd-accent);
  line-height: 1.1;
}
.stat-label { font-size: 24rpx; color: var(--cd-t3); margin-top: 4rpx; }
.stat-divider { width: 1rpx; height: 64rpx; background: var(--cd-line); }

/* 信箱 */
.letter-card {
  display: flex;
  align-items: center;
  gap: 24rpx;
  background: linear-gradient(135deg, var(--cd-bg), var(--cd-surface2));
}
.letter-ready { background: linear-gradient(135deg, var(--cd-surface2), var(--cd-accent)); }
.letter-icon { font-size: 56rpx; }
.letter-body { flex: 1; }
.letter-title { font-size: 30rpx; font-weight: 600; color: var(--cd-t1); }
.letter-sub { font-size: 24rpx; color: var(--cd-t2); margin-top: 4rpx; }

/* 菜单 */
.menu-card { padding: 0; overflow: hidden; }
.menu-item {
  display: flex;
  align-items: center;
  padding: 28rpx 32rpx;
}
.menu-icon { font-size: 32rpx; margin-right: 20rpx; }
.menu-label { flex: 1; font-size: 28rpx; color: var(--cd-t1); }
.menu-arrow { color: var(--cd-t3); font-size: 32rpx; }
.menu-divider { height: 1rpx; background: var(--cd-surface2); margin-left: 84rpx; }

/* 邀请按钮 */
.invite-btn {
  margin: 8rpx 32rpx 24rpx;
  background: var(--cd-accent);
  color: var(--cd-surface);
  border-radius: 24rpx;
  padding: 28rpx 0;
  text-align: center;
  font-size: 30rpx;
  font-weight: 500;
}
.invite-icon { margin-right: 12rpx; }

.bottom-pad { height: 120rpx; }

.menu-value {
  font-size: 26rpx;
  color: var(--cd-t3);
  margin-right: 8rpx;
}
